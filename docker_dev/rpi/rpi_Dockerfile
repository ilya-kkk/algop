ARG from_image
FROM ilya9kkk/ros_arm:camera

RUN apt-get update && apt-get install -y \
    python3 \
    python3-pip \
    python3-rpi.gpio \
    ros-noetic-std-msgs \
    ros-noetic-rospy

# Устанавливаем Python-библиотеки
RUN pip3 install --no-cache-dir \
    RPi.GPIO 

